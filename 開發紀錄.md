# 永續報告書 GRI 分析工具 — 開發紀錄

## 專案資訊

| 項目 | 內容 |
|------|------|
| 專案名稱 | 永續報告書 GRI 分析工具 |
| GitHub | https://github.com/carolkao-fin/gri-analyzer |
| 部署平台 | Streamlit Cloud |
| 開發工具 | Claude Code (claude-sonnet-4-6) |
| 技術架構 | Python · Streamlit · Groq API · pdfplumber |

---

## 功能說明

使用者上傳一份或多份永續報告書 PDF，系統自動分析各章節重點並對應 GRI Standards 2021 官方標準，輸出：
- 報告書概覽（公司、年度、框架、查證）
- 重大議題清單（GRI 3）
- 各章節核心重點、關鍵數據、對應 GRI 代碼
- GRI 符合度摘要（完整 / 部分 / 建議補強）
- 整體評分（優 / 良 / 中 / 待改善）

---

## 開發歷程

### v1 — 初始架構（Anthropic API）

**技術選擇**
- LLM：Anthropic Claude `claude-sonnet-4-6`
- PDF 解析：pdfplumber
- API Key：Streamlit secrets

**問題**
- Anthropic API 需要付費，不符合免費部署需求

---

### v2 — 改用 Google Gemini API

**變更**
- LLM 改為 `google-generativeai`，模型 `gemini-1.5-flash`
- API Key 改為使用者於 Sidebar 自行輸入（不需設定 Streamlit secrets）
- 新增 `gri_reference.py`：內建完整 GRI 2021 官方標準對照表
- 申請連結：https://aistudio.google.com/app/apikey

**問題**
- 出現 `google.api_core.exceptions.ResourceExhausted`（HTTP 429）
- 原因：Prompt 過大（PDF 全文 + GRI 對照表），超出 Gemini 免費層限制

---

### v3 — 改用 Groq API

**變更**
- LLM 改為 `groq`，模型 `llama-3.3-70b-versatile`
- MAX_CHARS 縮減至 25,000
- 加入自動重試邏輯（rate limit 時等待 60 秒後重試一次）
- 申請連結：https://console.groq.com/keys

**問題**
- `llama-3.3-70b-versatile` 免費層 TPM 僅 6,000 tokens/min
- 中文字元約 2 tokens/字，25,000 字元 ≈ 50,000 tokens，遠超上限

---

### v4 — 換模型 + 智慧選頁

**變更**
- 模型改為 `llama-3.1-8b-instant`（TPM 上限 20,000）
- MAX_CHARS 縮減至 6,000
- max_tokens 縮減至 2,048
- PDF 擷取改為「前 10 頁 + 後 12 頁」（概覽 + GRI 索引）

**問題**
- 只讀前後頁，跳過中間 ESG 章節，分析準確度不足

---

### v5 — 全文關鍵字篩選（當前版本）

**核心設計**

捨棄頁碼選取，改從**整份 PDF 全文**中，只保留包含 GRI 技術詞彙的行：

| 類別 | 關鍵字範例 |
|------|-----------|
| GRI 代碼 | `GRI`、`302-`、`305-`、`403-` |
| 氣候數據 | `tCO2e`、`MWh`、`Scope 1`、`溫室氣體` |
| 水資源/廢棄物 | `廢水排放`、`耗水量`、`廢棄物產生` |
| 社會 | `職業傷害`、`重大議題`、`強迫勞動` |
| 框架標準 | `AA1000`、`SASB`、`SBTi`、`CDP`、`RBA` |

**關鍵修正**：移除過於廣泛的關鍵字（`%`、`萬`、`億`、`員工` 等），這些詞在報告書幾乎每行都有，導致篩選等於沒做。

**變更**
- `extract_pdf_text()`：讀取全部頁面，透過 `filter_gri_lines()` 篩選
- UI 顯示「篩選後送出 X 字元」，方便確認 token 用量

---

## 技術限制說明

**Groq 免費層 Token 限制**

| 模型 | TPM 上限 | 適用中文字元上限 |
|------|---------|----------------|
| llama-3.3-70b-versatile | 6,000 | ≈ 1,800 字元 |
| llama-3.1-8b-instant | 20,000 | ≈ 6,000 字元 |

中文字元在 LLaMA tokenizer 約 2 tokens/字，因此實際可用文字遠少於英文。

---

## 檔案結構

```
gri-analyzer/
├── app.py              # 主程式（Streamlit UI + Groq API 呼叫）
├── gri_reference.py    # GRI 2021 官方標準完整對照表
├── requirements.txt    # streamlit, groq, pdfplumber
├── 開發紀錄.md
└── .streamlit/
    └── secrets.toml.example
```

---

## GRI 官方標準來源

- `gri_reference.py` 內建 GRI Universal Standards 2021（GRI 2、GRI 3）及 Topic Standards（GRI 201–418）
- 來源：https://www.globalreporting.org/standards/
- 模型只能對應此清單中的代碼，每個代碼附官方英文名稱

---

## 待改善項目

- [ ] 中文 PDF token 問題：考慮多輪呼叫（chunking）以涵蓋更多內容
- [ ] 分析準確度驗證：與人工 GRI 對照結果比較
- [ ] 支援輸出 Excel 格式的分析報告
