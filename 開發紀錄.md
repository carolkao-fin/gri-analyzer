# 台灣專利查詢工具 — 開發紀錄

## 專案資訊

| 項目 | 內容 |
|------|------|
| 專案名稱 | 台灣專利查詢工具（patent_search） |
| GitHub | https://github.com/carolkao-fin/patent-search |
| 部署平台 | Streamlit Cloud |
| 開發工具 | Claude Code (claude-sonnet-4-6) |
| 技術架構 | Python · Streamlit · requests · BeautifulSoup · openpyxl |

---

## 功能說明

使用者輸入公司清單，系統自動以公司名稱為關鍵字，向中華民國智慧財產局 TIPO TWPAT 搜尋系統查詢專利，並輸出 Excel 報表。

### 主要功能
- 公司清單輸入（逐筆或批次換行）
- 搜尋條件設定（申請人、當前專利權人等欄位 + AND/OR/NOT）
- IPC / LOC 專屬搜尋列
- 日期類型與範圍設定（yyyymmdd 格式）
- 專利類型篩選（發明／新型／設計）
- 排序選項
- 結果輸出為 Excel（各公司分開 ZIP 或合併）
- TIPO 公開資訊 API 模式（帳號密碼認證）
- 自訂 API 模式

---

## 開發歷程

### v1 初版（commit `9295197`）
- 建立 Streamlit 基礎框架
- 實作 TIPO TWPAT 網頁爬蟲
- Excel 輸出功能

### v2（commit `d6a1707`）
- 模仿 TIPO TWPAT 介面設計搜尋表單
- 支援多公司查詢
- 加入自訂 API 支援

### v3（commit `d34787c`）
- 將所有 UI 移至主區域（移除側邊欄）

### v4（commit `9867106`）
- 完整對應 TIPO 搜尋欄位清單
- AND/OR/NOT 移至左側（符合 TIPO 介面）
- IPC / LOC 獨立搜尋列

### v5（commit `b437fa5`）
- 設計改版：公司清單自動作為關鍵字
- 搜尋條件改為「欄位選擇」而非輸入框
- `build_query()` 重構：條件只有 field + op，keyword 自動取自公司名稱

### v6（commit `e60146a`）
- 日期欄位改為 yyyymmdd 文字輸入（對應 TIPO 格式）
- 對應 TIPO 截圖的日期類型選項

### UI 調整（commit `918ba03`）
- 將「公司清單」區塊移到「搜尋條件」上方

---

## 技術挑戰與解法

### 1. TIPO TWPAT Anti-Proxy（TTS_AntiProxy）

**問題**：TIPO TWPAT 網站使用 TTS_AntiProxy 防爬蟲保護，直接 POST 請求回傳 403。

**解法歷程**：
1. `0bc9384`：強化 HTTP headers，加入完整 Chrome 瀏覽器標頭（User-Agent、Sec-Ch-Ua、Sec-Fetch-* 等）
2. `f9a7a4c`：加入 `_solve_antiproxy()`，先 GET 搜尋頁取得 session cookie，並解析 JS `document.cookie` 注入 session
3. `b127faf`：發現 TTS 允許 GET 但封鎖 POST，改用 GET + query string 發送搜尋請求
4. `a738f95`：加入首頁暖機、最多 3 次重試、TIPO 錯誤頁偵測（K050、SystemMessage 等）

### 2. Brotli 壓縮亂碼

**問題**：在 `Accept-Encoding` 加入 `br`（brotli）後，requests 無法解壓縮，BeautifulSoup 收到二進位亂碼。

**解法（commit `3945b40`）**：移除 `br`，僅保留 `gzip, deflate`（requests 內建支援）。

### 3. UTF-8 亂碼

**問題**：TIPO 回應 UTF-8，但 requests 預設以 Latin-1 解碼，中文顯示為 `ç³»çµ±ç¼çç°å¸¸`。

**解法（commit `a738f95`）**：所有 `sess.get()` 後加入 `r.encoding = 'utf-8'`；新增 `_get_utf8()` helper。

### 4. 查詢參數名稱錯誤（2048 筆問題）

**問題**：POST payload 使用 `query` 作為搜尋字串參數名，但 TIPO 實際使用 `kw`；伺服器忽略參數，回傳全部結果（2048 筆）。

**解法（commit `75e914c`）**：
- 加入 `_PARAM_CANDIDATES` 字典，列出各邏輯參數的候選名稱（kw 優先）
- `_resolve_param()`：從偵測到的表單欄位自動找最佳匹配
- `_parse_form()`：解析 TIPO 頁面的 `<form>` 取得實際欄位名稱清單

### 5. 表格欄位錯位（明細亂碼）

**問題**：TIPO 結果表格第 0 欄為序號，原本程式從第 0 欄取公告號，全部錯位。

**解法（commit `75e914c`）**：
- `_detect_cols()`：從表頭行動態偵測欄位索引
- fallback：若表頭偵測失敗，預設第 0 欄為序號（offset +1）

### 6. TIPO 公開資訊 API 認證

**問題**：使用者收到 API 帳號密碼，但 `getAuth` 回傳純文字 JWT（非 JSON），解析時出現 `Extra data: line 1 column 3` 錯誤。

**解法（commit `ee9781a`）**：先嘗試 `r.json()`，失敗時改用 `r.text.strip()` 作為 token。

認證流程：
```
帳號:密碼 → Base64 → Authorization: Basic <token>
→ GET /S092_API/opd1/getAuth
→ 取得 Bearer Token
→ Authorization: Bearer <token> 查詢案件
```

> 注意：TIPO 公開資訊 API 僅支援以案號查詢，不支援以申請人名稱搜尋。

---

## 已知限制

| 限制 | 說明 |
|------|------|
| Streamlit Cloud 部署 | Anti-Proxy 會封鎖來自雲端伺服器的請求，建議在本機執行 |
| TIPO API 搜尋 | 公開資訊 API 無申請人搜尋端點，僅可依案號查詢 |
| 動態 JavaScript | TIPO 若以 JS 動態載入結果，requests 爬蟲將無法取得資料 |

---

## 相依套件

```
streamlit>=1.32.0
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
openpyxl>=3.1.0
pandas>=2.0.0
```

---

## 檔案結構

```
patent_search.py   主程式（UI + 爬蟲 + Excel 輸出）
requirements.txt   相依套件
開發紀錄.md        本文件
```
